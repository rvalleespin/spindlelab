# Ilustraciones de marca

## hero-inicio-v2.svg

Segunda versión de la ilustración del hero del home, dibujada el 27-jul-2026 durante el rediseño. **Nunca se aprobó ni se publicó.**

La v1 sí llegó a estar en el sitio (`spindlelab-site/assets/img/illustrations/hero-inicio.svg`, commit `0248b97`) y se retiró dos días después en la Ronda 1 del rediseño (`a2a55cc`), junto con el resto de esa carpeta.

Se rescata aquí el 21-sep-2026 porque su único respaldo eran tres commits sueltos de stash (`b30d413`, `d73e39d`, `dd8c8a7`, titulados "WIP: hero illustration v2 (awaiting user approval)") más una copia en `_cuarentena-ronda-01/`, que está fuera de git por su propio `.gitignore`. Un `git gc` se habría llevado los stash y habría quedado sin respaldo versionado.

Diferencias con la v1: viewBox 600×500 en vez de 560×480, 23 paths en vez de 14, la figura completa con sombra de piso en vez de cortada por el borde inferior, y una línea de "hilo conductor" que recorre la composición.

Queda archivada, no en uso: el sitio publicado hoy es Astro y ya no consume estos SVG.
